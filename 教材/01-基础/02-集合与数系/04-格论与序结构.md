# 格论与序结构

---

### 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）**# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
-# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）**# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P,# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L,# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee,# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x,# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complement# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L =# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S =# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\big# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开集族为完备格。

**定理 88# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开集族为完备格。

**定理 88.3.3（Knaster-Tarski不动点定理）** 设 $(L, \# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开集族为完备格。

**定理 88.3.3（Knaster-Tarski不动点定理）** 设 $(L, \leq)$ 为完备格，$f: L \to L$ 为保序映射。则 $f$ 的不动点集合# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开集族为完备格。

**定理 88.3.3（Knaster-Tarski不动点定理）** 设 $(L, \leq)$ 为完备格，$f: L \to L$ 为保序映射。则 $f$ 的不动点集合 $\operatorname{Fix}(f) = \{x \in L : f(x) = x\}$ 非空，且在诱导序下构成一个完备格。特别地，$f$# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开集族为完备格。

**定理 88.3.3（Knaster-Tarski不动点定理）** 设 $(L, \leq)$ 为完备格，$f: L \to L$ 为保序映射。则 $f$ 的不动点集合 $\operatorname{Fix}(f) = \{x \in L : f(x) = x\}$ 非空，且在诱导序下构成一个完备格。特别地，$f$ 有最小不动点和最大不动点。

**证明**：我们给出完整证明。

设 $# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开集族为完备格。

**定理 88.3.3（Knaster-Tarski不动点定理）** 设 $(L, \leq)$ 为完备格，$f: L \to L$ 为保序映射。则 $f$ 的不动点集合 $\operatorname{Fix}(f) = \{x \in L : f(x) = x\}$ 非空，且在诱导序下构成一个完备格。特别地，$f$ 有最小不动点和最大不动点。

**证明**：我们给出完整证明。

设 $A = \{x \in L : x \leq f(x)\}$。由于 $0 \in A$（$0 \leq f(0)$），$A$ 非空。令 $a = \bigvee A$。# 格论与序结构

---

## 第88章：偏序集与格

格论（Lattice Theory）是序结构与代数结构交汇的数学分支，其根源可追溯至Dedekind关于理想的研究和Peirce关于关系代数的探索。1930年代，Birkhoff的经典著作奠定了现代格论的基础。本章系统阐述偏序集、格、完备格以及相关的代数结构与逻辑联系。

### 88.1 偏序集的基本概念

**定义 88.1.1（偏序集）** 设 $P$ 为一个集合，$\leq$ 是 $P$ 上的二元关系。若 $\leq$ 满足以下条件，则称 $(P, \leq)$ 为一个偏序集（partially ordered set，简称poset）：

1. （自反性）对所有 $x \in P$，$x \leq x$；
2. （反对称性）若 $x \leq y$ 且 $y \leq x$，则 $x = y$；
3. （传递性）若 $x \leq y$ 且 $y \leq z$，则 $x \leq z$。

若对任意 $x, y \in P$，总有 $x \leq y$ 或 $y \leq x$，则称 $\leq$ 为全序（total order）或线性序（linear order），$(P, \leq)$ 称为全序集（totally ordered set）或链（chain）。

**定义 88.1.2（严格偏序）** 严格偏序 $<$ 定义为：$x < y$ 当且仅当 $x \leq y$ 且 $x \neq y$。严格偏序满足非自反性和传递性。

**定义 88.1.3（覆盖关系）** 设 $(P, \leq)$ 为偏序集，$x, y \in P$。若 $x < y$ 且不存在 $z \in P$ 使得 $x < z < y$，则称 $y$ 覆盖 $x$（或 $x$ 被 $y$ 覆盖），记作 $x \prec y$。

**定义 88.1.4（Hasse图）** 偏序集 $(P, \leq)$ 的Hasse图是一个有向无环图的图形表示，其中：
- 每个元素 $x \in P$ 对应平面上的一个点；
- 若 $x \prec y$，则画一条从 $x$ 到 $y$ 的线段（$y$ 位于 $x$ 的上方）。

Hasse图完全确定了有限偏序集的结构，因为 $x \leq y$ 当且仅当存在一条从 $x$ 到 $y$ 的上升路径。Hasse图是可视化偏序集的最有力工具。

**定义 88.1.5（极大元、极小元、最大元、最小元）** 设 $(P, \leq)$ 为偏序集。

- $m \in P$ 为极大元，若不存在 $x \in P$ 使得 $m < x$；
- $m \in P$ 为极小元，若不存在 $x \in P$ 使得 $x < m$；
- $m \in P$ 为最大元，若对所有 $x \in P$，$x \leq m$；
- $m \in P$ 为最小元，若对所有 $x \in P$，$m \leq x$。

最大元和最小元若存在，必唯一。极大元和极小元不一定唯一。

**定义 88.1.6（上界、下界、上确界、下确界）** 设 $(P, \leq)$ 为偏序集，$S \subseteq P$。

- $u \in P$ 为 $S$ 的上界，若对所有 $s \in S$，$s \leq u$；
- $\ell \in P$ 为 $S$ 的下界，若对所有 $s \in S$，$\ell \leq s$；
- $S$ 的所有上界构成的集合若有最小元，则称其为 $S$ 的上确界（supremum），记作 $\bigvee S$ 或 $\sup S$；
- $S$ 的所有下界构成的集合若有最大元，则称其为 $S$ 的下确界（infimum），记作 $\bigwedge S$ 或 $\inf S$。

当 $S = \{x, y\}$ 时，记 $x \vee y = \sup\{x, y\}$，$x \wedge y = \inf\{x, y\}$。

**定义 88.1.7（良序）** 设 $(P, \leq)$ 为全序集。若 $P$ 的每个非空子集都有最小元，则称 $(P, \leq)$ 为良序集（well-ordered set），$\leq$ 为 $P$ 上的良序。

**定理 88.1.8（Zorn引理）** 设 $(P, \leq)$ 为非空偏序集。若 $P$ 中的每条链都有上界，则 $P$ 中至少有一个极大元。

Zorn引理等价于选择公理，是数学中构造极大对象的基本工具。其证明依赖于选择公理和超限归纳法。

**命题 88.1.9（对偶原理）** 若 $\Phi$ 是偏序集理论中为真的陈述，则其对偶陈述 $\Phi^{\partial}$（将 $\leq$ 替换为 $\geq$，将 $\vee$ 替换为 $\wedge$，将 $\sup$ 替换为 $\inf$ 等）亦为真。

**定义 88.1.10（保序映射与序同构）** 设 $(P, \leq_P)$ 和 $(Q, \leq_Q)$ 为偏序集。映射 $f: P \to Q$ 称为保序的（order-preserving）或单调的，若 $x \leq_P y$ 蕴涵 $f(x) \leq_Q f(y)$。若 $f$ 为双射且 $f$ 和 $f^{-1}$ 均保序，则称 $f$ 为序同构（order-isomorphism）。

**定义 88.1.11（Dilworth定理）** 设 $(P, \leq)$ 为有限偏序集。$P$ 可分解为 $k$ 个两两不交的链之并的最小 $k$ 等于 $P$ 中反链（两两不可比元素的子集）的最大基数。

Dilworth定理是组合序理论中的核心结果，与匹配理论、Hall婚配定理有深刻联系。

**定义 88.1.12（理想与滤子）** 设 $(P, \leq)$ 为偏序集。

- 子集 $I \subseteq P$ 称为序理想（order ideal）或下集（down-set），若 $x \in I$ 且 $y \leq x$ 蕴涵 $y \in I$；
- 子集 $F \subseteq P$ 称为序滤子（order filter）或上集（up-set），若 $x \in F$ 且 $x \leq y$ 蕴涵 $y \in F$。

$P$ 的所有序理想构成的集合 $O(P)$ 在包含关系下构成一个完备格。

### 88.2 格的定义与基本性质

**定义 88.2.1（格——序定义）** 偏序集 $(L, \leq)$ 称为格（lattice），若对任意 $x, y \in L$，$x \vee y$ 和 $x \wedge y$ 均存在。

**定义 88.2.2（格——代数定义）** 格是一个代数结构 $(L, \vee, \wedge)$，其中 $\vee$ 和 $\wedge$ 是 $L$ 上的二元运算，满足以下恒等式：

1. （幂等律）$x \vee x = x$，$x \wedge x = x$；
2. （交换律）$x \vee y = y \vee x$，$x \wedge y = y \wedge x$；
3. （结合律）$(x \vee y) \vee z = x \vee (y \vee z)$，$(x \wedge y) \wedge z = x \wedge (y \wedge z)$；
4. （吸收律）$x \vee (x \wedge y) = x$，$x \wedge (x \vee y) = x$。

序定义和代数定义等价：若 $(L, \vee, \wedge)$ 满足上述代数条件，定义 $x \leq y \iff x \vee y = y$（等价于 $x \wedge y = x$），则 $(L, \leq)$ 为格且 $\vee$、$\wedge$ 分别为并、交运算。

**命题 88.2.3（格的基本性质）** 在任意格 $L$ 中，对任意 $x, y, z \in L$：

1. $x \leq y$ 蕴涵 $x \vee z \leq y \vee z$ 和 $x \wedge z \leq y \wedge z$；
2. $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$；
3. $(x \wedge y) \vee (x \wedge z) \leq x \wedge (y \vee z)$。

**证明**：（1）因 $y \leq y \vee z$ 且 $x \leq y \leq y \vee z$，又 $z \leq y \vee z$，故 $x \vee z \leq y \vee z$。下确界情形类似。（2）$x \leq x \vee y$ 且 $x \leq x \vee z$，故 $x \leq (x \vee y) \wedge (x \vee z)$；又 $y \wedge z \leq y \leq x \vee y$ 且 $y \wedge z \leq z \leq x \vee z$，故 $y \wedge z \leq (x \vee y) \wedge (x \vee z)$。因此 $x \vee (y \wedge z) \leq (x \vee y) \wedge (x \vee z)$。（3）的证明与（2）对偶。$\blacksquare$

**定义 88.2.4（分配格）** 格 $L$ 称为分配格（distributive lattice），若对任意 $x, y, z \in L$，满足分配律：

$$x \vee (y \wedge z) = (x \vee y) \wedge (x \vee z),$$
$$x \wedge (y \vee z) = (x \wedge y) \vee (x \wedge z).$$

实际上，由对偶原理，上述两个恒等式等价。

**定理 88.2.5（分配格的刻画）** 格 $L$ 是非分配的当且仅当 $L$ 包含一个子格同构于 $M_3$（钻石格）或 $N_5$（五边形格）。

**证明概要**：$M_3$ 和 $N_5$ 是仅有的两个极小非分配格。若 $L$ 不包含 $M_3$ 或 $N_5$ 作为子格，则可证明 $L$ 是分配的；反之，若 $L$ 包含其中之一，则分配律在此子格上失效，因而在全格上也失效。$\blacksquare$

**定义 88.2.6（模格）** 格 $L$ 称为模格（modular lattice），若对任意 $x, y, z \in L$，满足模律：

$$x \leq z \implies x \vee (y \wedge z) = (x \vee y) \wedge z.$$

每个分配格都是模格，但反之不然。$M_3$ 是模格但非分配格，而 $N_5$ 既非分配亦非模格。Dedekind在研究理想时首次引入模格概念。

**定理 88.2.7（模格的刻画）** 格 $L$ 是模格当且仅当 $L$ 不包含同构于 $N_5$ 的子格。

**定义 88.2.8（补元与有补格）** 设 $L$ 为具有最小元 $0$ 和最大元 $1$ 的格。元素 $x \in L$ 的补元（complement）是满足 $x \vee y = 1$ 且 $x \wedge y = 0$ 的 $y \in L$。若 $L$ 中每个元素都有补元，则称 $L$ 为有补格（complemented lattice）。在有补分配格中，每个元素的补元唯一，这样的格就是Bool代数。

**定理 88.2.9（Birkhoff表示定理）** 每个有限分配格同构于某个偏序集的所有序理想的格。

**证明概要**：设 $L$ 为有限分配格。令 $J(L)$ 为 $L$ 的并不可约元（join-irreducibles）构成的子偏序集。定义映射 $\phi: L \to O(J(L))$ 为 $\phi(x) = \{j \in J(L) : j \leq x\}$。则 $\phi$ 为格同构。这建立了有限分配格与有限偏序集之间的一一对应。$\blacksquare$

**定义 88.2.10（同余与商格）** 格 $L$ 上的一个同余关系（congruence）是 $L$ 上的等价关系 $\theta$，满足：若 $a \theta b$ 且 $c \theta d$，则 $(a \vee c) \theta (b \vee d)$ 且 $(a \wedge c) \theta (b \wedge d)$。$L$ 关于 $\theta$ 的商 $L/\theta$ 自然地构成格。

**定理 88.2.11（格同态基本定理）** 若 $f: L \to M$ 为格满同态，则 $\ker f = \{(x, y): f(x) = f(y)\}$ 是 $L$ 上的同余，且 $L/\ker f \cong M$。

### 88.3 完备格与不动点定理

**定义 88.3.1（完备格）** 偏序集 $(L, \leq)$ 称为完备格（complete lattice），若 $L$ 的每个子集 $S \subseteq L$ 都有上确界 $\bigvee S$ 和下确界 $\bigwedge S$。

完备格必有最大元 $\bigvee L = 1$ 和最小元 $\bigwedge L = 0$（取 $S = \varnothing$ 和 $S = L$）。

**例子 88.3.2** （1）任意集合 $X$ 的幂集 $(\mathcal{P}(X), \subseteq)$ 为完备格，$\bigvee = \bigcup$，$\bigwedge = \bigcap$。（2）任意群的所有子群构成的集合在包含关系下为完备格。（3）实闭区间 $[0,1]$ 在通常序下为完备格。（4）所有拓扑空间的开集族为完备格。

**定理 88.3.3（Knaster-Tarski不动点定理）** 设 $(L, \leq)$ 为完备格，$f: L \to L$ 为保序映射。则 $f$ 的不动点集合 $\operatorname{Fix}(f) = \{x \in L : f(x) = x\}$ 非空，且在诱导序下构成一个完备格。特别地，$f$ 有最小不动点和最大不动点。

**证明**：我们给出完整证明。

设 $A = \{x \in L : x \leq f(x)\}$。由于 $0 \in A$（$0 \leq f(0)$），$A$ 非空。令 $a = \bigvee A$。我们证明 $a$ 是 $f$ 的不动点。

首先，对任意 $x \in A$，有 $x \