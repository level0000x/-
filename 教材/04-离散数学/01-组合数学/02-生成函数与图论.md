## 第37章：生成函数

### 37.1 普通生成函数

**定义 37.1**（普通生成函数）：设 $\{a_n\}_{n=0}^\infty$ 为数域 $\mathbb{C}$ 中的序列。其**普通生成函数**（Ordinary Generating Function, OGF）为形式幂级数
$$F(x) = \sum_{n=0}^\infty a_n x^n \in \mathbb{C}[[x]]$$
若该级数在某开圆盘 $|x| < R$ 内收敛，其中收敛半径 $R = 1 / \limsup_{n\to\infty} \sqrt[n]{|a_n|}$，则 $F(x)$ 同时定义了一个全纯函数。若 $R = 0$，则 $F(x)$ 仅为形式幂级数；若 $R = \infty$，则 $F(x)$ 为整函数。

**定理 37.1**（OGF 加法定理）：设 $\{a_n\}$ 和 $\{b_n\}$ 的 OGF 分别为 $A(x)$ 和 $B(x)$，则序列 $\{a_n + b_n\}$ 的 OGF 为 $A(x) + B(x)$。

**证明**：由定义 $\sum_{n\geq 0}(a_n + b_n) x^n = \sum_{n\geq 0} a_n x^n + \sum_{n\geq 0} b_n x^n = A(x) + B(x)$。$\blacksquare$

**定理 37.2**（OGF 乘法定理 / Cauchy 卷积）：序列 $\{a_n\}$ 和 $\{b_n\}$ 的 OGF 之积 $A(x)B(x) = \sum_{n\geq 0} c_n x^n$，其中
$$c_n = \sum_{k=0}^n a_k b_{n-k}$$
称为 $\{a_n\}$ 与 $\{b_n\}$ 的 **Cauchy 卷积**。

**证明**：由级数乘法 $(\sum_{i\geq 0} a_i x^i)(\sum_{j\geq 0} b_j x^j) = \sum_{n\geq 0} (\sum_{i+j=n} a_i b_j) x^n$。令 $k = i$ 即得 $c_n = \sum_{k=0}^n a_k b_{n-k}$。$\blacksquare$

**定理 37.3**（OGF 的复合）：若 $B(0) = 0$，则形式幂级数的复合 $A(B(x)) = \sum_{n\geq 0} a_n (B(x))^n$ 是良定义的。组合意义上，若 $A(x)$ 和 $B(x)$ 分别对应组合类 $\mathcal{A}$ 与 $\mathcal{B}$ 的 OGF，且 $\mathcal{B}$ 无大小为 $0$ 的元素，则 $A(B(x))$ 对应 $\mathcal{A}$ 中元素由 $\mathcal{B}$ 中元素代换而成的结构。

### 37.2 指数生成函数

**定义 37.2**（指数生成函数）：序列 $\{a_n\}$ 的**指数生成函数**（Exponential Generating Function, EGF）定义为
$$\hat{F}(x) = \sum_{n=0}^\infty a_n \frac{x^n}{n!}$$

EGF 的乘积对应于**二项卷积**，使其适用于**标记结构**（labeled structures）的计数。

**定理 37.4**（EGF 乘法公式）：设 $\hat{A}(x), \hat{B}(x)$ 分别为 $\{a_n\}$ 和 $\{b_n\}$ 的 EGF，则
$$\hat{A}(x)\hat{B}(x) = \sum_{n=0}^\infty \left(\sum_{k=0}^n \binom{n}{k} a_k b_{n-k}\right) \frac{x^n}{n!}$$

**证明**：$\hat{A}(x)\hat{B}(x) = (\sum_{k} a_k \frac{x^k}{k!})(\sum_{j} b_j \frac{x^j}{j!}) = \sum_{n} (\sum_{k=0}^n \frac{a_k b_{n-k}}{k!(n-k)!}) x^n = \sum_{n} (\sum_{k=0}^n \binom{n}{k} a_k b_{n-k}) \frac{x^n}{n!}$。$\blacksquare$

组合诠释：在标记组合的符号方法中，若 $\mathcal{A}$ 和 $\mathcal{B}$ 为标记组合类，它们的**标记乘积** $\mathcal{A} * \mathcal{B}$ 的 EGF 恰为 $\hat{A}(x)\hat{B}(x)$。

### 37.3 线性递推的生成函数解法

**定理 37.5**（常系数线性齐次递推）：设序列 $\{a_n\}$ 满足递推 $a_{n+k} = c_1 a_{n+k-1} + \cdots + c_k a_n$（$n \geq 0$），$c_i \in \mathbb{C}$，初值 $a_0, \ldots, a_{k-1}$ 给定。定义特征多项式（以 $x$ 为变量）
$$P(x) = 1 - c_1 x - \cdots - c_k x^k$$
则 $\{a_n\}$ 的 OGF 为 $F(x) = Q(x) / P(x)$，其中 $Q(x)$ 是由初值确定的次数小于 $k$ 的多项式。

**证明**：计算 $P(x)F(x) = (1 - \sum_{i=1}^k c_i x^i)(\sum_{n\geq 0} a_n x^n) = \sum_{n\geq 0} a_n x^n - \sum_{i=1}^k c_i \sum_{n\geq 0} a_n x^{n+i}$。将第二项中 $n$ 替换为 $n-i$ 得 $\sum_{n\geq 0} (a_n - \sum_{i=1}^k c_i a_{n-i}) x^n$，其中约定 $a_j=0$ 当 $j<0$。对 $n \geq k$，递推关系表明括号内为 $0$。故 $P(x)F(x)$ 为次数小于 $k$ 的多项式 $Q(x)$。$\blacksquare$

**定理 37.6**（部分分式与闭式）：若特征多项式有因式分解 $P(x) = \prod_{i=1}^r (1-\lambda_i x)^{m_i}$（$\lambda_i$ 互异），则
$$a_n = \sum_{i=1}^r P_{m_i-1}^{(i)}(n) \lambda_i^n$$
其中 $P_{d}^{(i)}(n)$ 是次数不超过 $d$ 的多项式。当 $m_i = 1$（即单根）时，简化为指数项 $C_i \lambda_i^n$。

### 37.4 Catalan 数的生成函数

**定义 37.3**（Catalan 数）：第 $n$ 个 **Catalan 数** $C_n$ 满足 $C_0 = 1$ 及递推 $C_{n+1} = \sum_{k=0}^n C_k C_{n-k}$（$n \geq 0$）。$C_n$ 计数 $n$ 对括号的正确配对方式、$n+1$ 个叶子的二叉树数目、$\{1,\ldots,2n\}$ 的 $n$ 个不交叉弧的匹配等。

**定理 37.7**（Catalan 生成函数与闭式）：Catalan 数的 OGF $C(x) = \sum_{n\geq 0} C_n x^n$ 满足
$$C(x) = \frac{1 - \sqrt{1 - 4x}}{2x}$$
且闭式为 $C_n = \frac{1}{n+1} \binom{2n}{n}$。

**证明**：由递推 $C_{n+1} = \sum_{k=0}^n C_k C_{n-k}$ 得 $C(x) - 1 = \sum_{n\geq 0} C_{n+1} x^{n+1} = x \sum_{n\geq 0} (\sum_{k=0}^n C_k C_{n-k}) x^n = x C(x)^2$。故 $x C(x)^2 - C(x) + 1 = 0$。解二次方程得 $C(x) = (1 \pm \sqrt{1-4x})/(2x)$。由 $C(0) = C_0 = 1$，取负号：$C(x) = (1 - \sqrt{1-4x})/(2x)$。将 $\sqrt{1-4x}$ 按二项级数展开：
$$\sqrt{1-4x} = \sum_{n=0}^\infty \binom{1/2}{n}(-4x)^n = 1 - 2\sum_{n=1}^\infty \frac{1}{n}\binom{2n-2}{n-1} x^n$$
代入得 $C(x) = \frac{1}{x} \sum_{n=1}^\infty \frac{1}{n}\binom{2n-2}{n-1} x^n = \sum_{n=0}^\infty \frac{1}{n+1} \binom{2n}{n} x^n$。故 $C_n = \frac{1}{n+1}\binom{2n}{n}$。$\blacksquare$

### 37.5 分拆数的生成函数

**定义 37.4**（分拆函数）：$p(n)$ 为将正整数 $n$ 写成正整数之和的不同方式数（不计顺序）。约定 $p(0) = 1$。

**定理 37.8**（Euler 分拆生成函数）：
$$\sum_{n=0}^\infty p(n) x^n = \prod_{k=1}^\infty \frac{1}{1 - x^k}$$

**证明**：右端每个因子展开为几何级数 $\frac{1}{1-x^k} = 1 + x^k + x^{2k} + \cdots$。从第 $k$ 个因子选择 $x^{m_k k}$（$m_k \geq 0$），所有因子的乘积为 $x^{\sum_k m_k k}$。指数 $\sum_k m_k k$ 恰为将某个整数分解为部分 $k$ 重复 $m_k$ 次的一种方式，故 $x^n$ 的系数恰为所有可能序列 $\{m_k\}$ 满足 $\sum m_k k = n$ 的数目——即 $p(n)$。$\blacksquare$

**定理 37.9**（Euler 五角数定理）：
$$\prod_{n=1}^\infty (1 - x^n) = \sum_{k=-\infty}^\infty (-1)^k x^{k(3k-1)/2}$$
该恒等式给出了 $p(n)$ 的高效递推公式。

### 37.6 Dirichlet生成函数与Dirichlet卷积

**定义 37.5**（Dirichlet生成函数）：设 $\{a_n\}_{n=1}^\infty \subseteq \mathbb{C}$。其**Dirichlet生成函数**（DGF）为 Dirichlet 级数
$$D(s) = \sum_{n=1}^\infty \frac{a_n}{n^s}$$
若存在 $\sigma_0$ 使级数在 $\Re(s) > \sigma_0$ 绝对收敛，则 $D(s)$ 定义了半平面内的全纯函数。

**定理 37.10**（DGF 乘法与 Dirichlet 卷积）：设 $A(s), B(s)$ 分别为 $\{a_n\}, \{b_n\}$ 的 DGF，则
$$A(s)B(s) = \sum_{n=1}^\infty \frac{(a * b)_n}{n^s},\quad (a * b)_n = \sum_{d \mid n} a_d \cdot b_{n/d}$$
其中 $a * b$ 称为 **Dirichlet 卷积**，构成 $\mathbb{C}$ 上数论函数的交换结合代数（单位元 $\varepsilon(n) = [n = 1]$）。

**证明**：$A(s)B(s) = \sum_{i,j \geq 1} a_i b_j (ij)^{-s}$。令 $n = ij$，对固定 $n$，满足 $d \mid n$ 的因子对 $(d, n/d)$ 贡献 $\frac{a_d b_{n/d}}{n^s}$。故系数为 $\sum_{d \mid n} a_d b_{n/d}$。$\blacksquare$

**注记 37.2**：Riemann zeta 函数 $\zeta(s) = \sum_{n=1}^\infty 1/n^s$（$a_n \equiv 1$）是 DGF 的最重要例子。积性函数（$a_{mn} = a_m a_n$ 当 $\gcd(m,n)=1$）的 DGF 具有 Euler 乘积：
$$D(s) = \prod_{p} \left( \sum_{k=0}^\infty \frac{a_{p^k}}{p^{ks}} \right)$$
完全积性函数简化为 $\prod_p (1 - a_p p^{-s})^{-1}$。DGF 将 Dirichlet 卷积代数化——这是解析数论的基本工具。

## 第38章：Pólya 计数与图多项式

### 38.1 Pólya 计数定理

**定义 38.1**（群作用与轨道）：设有限群 $G$ 作用于有限集 $X$。对 $x \in X$，轨道为 $\operatorname{Orb}(x) = \{g x : g \in G\}$；稳定子群为 $G_x = \{g \in G : g x = x\}$。轨道-稳定子定理给出 $|G| = |\operatorname{Orb}(x)| \cdot |G_x|$。

**定理 38.1**（Burnside 引理）：$G$ 作用于 $X$ 的轨道数 $N$ 满足
$$N = \frac{1}{|G|} \sum_{g \in G} |\operatorname{Fix}(g)|$$
其中 $\operatorname{Fix}(g) = \{x \in X : g x = x\}$。

**证明**：对集合 $\{(g, x) \in G \times X : g x = x\}$ 进行双重计数。按 $g$ 求和得 $\sum_{g} |\operatorname{Fix}(g)|$；按 $x$ 求和得 $\sum_{x} |G_x|$。对每个轨道 $\mathcal{O}$，$\sum_{x \in \mathcal{O}} |G_x| = |\mathcal{O}| \cdot (|G|/|\mathcal{O}|) = |G|$。故 $\sum_{x \in X} |G_x| = N \cdot |G|$。因此 $N = \frac{1}{|G|} \sum_g |\operatorname{Fix}(g)|$。$\blacksquare$

**定义 38.2**（循环指标）：设 $G \leq S_n$。$g \in G$ 的**循环类型**为 $(c_1(g), \ldots, c_n(g))$，其中 $c_k(g)$ 为 $g$ 的循环分解中 $k$-循环的个数。$G$ 的**循环指标多项式**为
$$Z_G(t_1, \ldots, t_n) = \frac{1}{|G|} \sum_{g \in G} \prod_{k=1}^n t_k^{c_k(g)}$$

**定理 38.2**（Pólya 计数定理）：设有 $m$ 种颜色。用这些颜色对 $X$ 的元素着色的 $G$-不等价着色数为
$$Z_G(m, m, \ldots, m) = \frac{1}{|G|} \sum_{g \in G} m^{c(g)}$$
其中 $c(g) = \sum_{k=1}^n c_k(g)$ 为 $g$ 的循环总数。

**证明**：着色对应于函数 $f : X \to \{1,\ldots,m\}$。$G$ 通过 $(g \cdot f)(x) = f(g^{-1} x)$ 作用于着色集合。$f$ 被 $g$ 固定 $\iff$ $f$ 在 $g$ 的每个循环上取常值，故 $|\operatorname{Fix}(g)| = m^{c(g)}$。由 Burnside 引理即得。$\blacksquare$

### 38.2 图多项式

**定义 38.3**（色多项式）：图 $G = (V, E)$ 的**色多项式** $P_G(k)$ 为用至多 $k$ 种颜色对 $G$ 的顶点进行正常着色（相邻顶点不同色）的方式数。

**定理 38.3**（删边-缩边递推）：对任意边 $e \in E$，
$$P_G(k) = P_{G-e}(k) - P_{G/e}(k)$$

**证明**：设 $e = uv$。$G-e$ 的 $k$-着色的集合等于 $\{G$ 的正常着色$\} \cup \{uv$ 同色的 $G-e$ 着色$\}$。使 $u, v$ 同色的 $G-e$ 着色与 $G/e$（合并 $u, v$ 为一个顶点）的 $k$-着色一一对应。故 $P_{G-e}(k) = P_G(k) + P_{G/e}(k)$。移项即得。$\blacksquare$

**推论 38.4**：$P_G(k)$ 是次数为 $|V|$ 的整系数多项式，首项为 $k^{|V|}$，系数正负交替，常数项为 $0$。

**定义 38.4**（Tutte 多项式）：图 $G$ 的 **Tutte 多项式** $T_G(x, y)$ 通过以下递推唯一定义：
1. 若 $G$ 无边，则 $T_G(x, y) = 1$
2. 若 $e$ 为桥，则 $T_G(x, y) = x \cdot T_{G/e}(x, y)$
3. 若 $e$ 为环，则 $T_G(x, y) = y \cdot T_{G-e}(x, y)$
4. 否则（$e$ 非桥非环），$T_G(x, y) = T_{G-e}(x, y) + T_{G/e}(x, y)$

**定理 38.5**（Tutte 多项式与色多项式的联系）：设 $c(G)$ 为 $G$ 的连通分支数，则
$$P_G(k) = (-1)^{|V|-c(G)} k^{c(G)} T_G(1-k, 0)$$

**证明**：由归纳法及删边-缩边公式可验证该关系式满足两多项式的递推归约。当 $x = 1-k$ 且 $y = 0$ 时，Tutte 的递归规则约化为色多项式的递归规则：桥缩并贡献因子 $(1-k)$，环删除贡献因子 $0$，一般边分裂为删边与缩边之差。基底情形（无边图）两侧一致。$\blacksquare$

## 第39章：图论基础

### 39.1 图的正式定义与矩阵表示

**定义 39.1**（图与矩阵）：一个**简单图** $G = (V, E)$ 由非空有限顶点集 $V$ 和边集 $E \subseteq \binom{V}{2}$ 构成，记 $n = |V|$，$m = |E|$。**邻接矩阵** $A(G) = (a_{ij})$，$a_{ij} = 1_{v_i v_j \in E}$。**度矩阵** $D(G) = \operatorname{diag}(\deg(v_1), \ldots, \deg(v_n))$，$\deg(v) = |\{u : uv \in E\}|$。**Laplace 矩阵** $L(G) = D - A$ 对称半正定，零特征值重数等于连通分支数。

**定义 39.2**（图同构与自同构）：$G$ 与 $H$ **同构**（$G \cong H$）若存在双射 $\varphi : V(G) \to V(H)$ 使 $uv \in E(G) \iff \varphi(u)\varphi(v) \in E(H)$。$G$ 的**自同构群** $\operatorname{Aut}(G)$ 为全体自同构关于复合构成的群，忠实作用于 $V$ 故 $\operatorname{Aut}(G) \leq S_n$。例：$\operatorname{Aut}(K_n) = S_n$，$\operatorname{Aut}(C_n) = D_{2n}$。

### 39.2 连通性与 Menger 定理

**定义 39.3**（连通性）：$u, v$ **连通**若存在路连接。等价类为**连通分支**。$v$ 为**割点**若 $G - v$ 有更多连通分支；$e$ 为**桥**若 $G - e$ 有更多连通分支。无割点的极大连通子图为**块**。**点连通度** $\kappa(G)$ 为最小 $S \subseteq V$ 使 $G - S$ 不连通（$\kappa(K_n) = n-1$）；**边连通度** $\lambda(G)$ 类似定义。

**定理 39.1**（Menger 定理——点形式）：设 $s, t$ 不相邻。则 $s, t$ 间内部不相交路径的最大数等于分离 $s, t$ 的最小顶点数。

**证明**（框架）：设内部不相交路径最大数为 $p$，最小分离集大小为 $q$。$p \leq q$ 平凡。$p \geq q$ 对边数极小反例归纳：设 $S$ 为极小 $s$-$t$ 分离集。若存在 $x \in S$ 与 $s$ 和 $t$ 均相邻，删 $x$ 归纳。否则沿 $S$ 将图分解为 $s$-侧与 $t$-侧，分别应用归纳假设，合并后即得 $p \geq q$。$\blacksquare$

**定理 39.2**（Menger 定理——边形式）：$s, t$ 间边不交路径最大数等于分离 $s, t$ 的最小边数。此即容量全为 1 的网络中最大流 = 最小割定理。**推论**：$\kappa(G) \geq k \iff$ 任意两顶点间有 $k$ 条内部不相交路径；$\lambda(G) \geq k \iff$ 任意两顶点间有 $k$ 条边不交路径。

### 39.3 图着色与 Brooks 定理

**定义 39.4**（色数）：$G$ 的**正常 $k$-着色**为 $c : V \to [k]$ 使 $uv \in E \Rightarrow c(u) \neq c(v)$。**色数** $\chi(G)$ 为最小的 $k$。

**定理 39.3**（Brooks 定理）：连通简单图 $G$ 非完全图，最大度 $\Delta \geq 3$，则 $\chi(G) \leq \Delta$。

**证明**（框架）：非正则时取 $\deg(v) < \Delta$，$G - v$ 归纳 $\Delta$-着色，$v$ 至多 $\Delta-1$ 个邻点限制故可着色。正则时由非完全图可取不相邻 $x, y$ 使 $G - \{x, y\}$ 连通。按 DFS 生成树逆序贪心着色 $G - \{x, y\}$（每顶点至多 $\Delta-1$ 个已着色邻点），$x, y$ 各有 $\leq \Delta-1$ 个限制，故 $\chi(G) \leq \Delta$。$\blacksquare$

### 39.4 平面图与 Kuratowski 定理

**定义 39.5**（平面图）：$G$ 为**平面图**若可嵌入 $\mathbb{R}^2$ 使边仅交于顶点。嵌入划分平面为 $f$ 个**面**。

**定理 39.4**（Euler 公式）：连通平面嵌入满足 $n - e + f = 2$。

**证明**：对 $e$ 归纳。$e = 0$ 时 $n = 1, f = 1$。$e \geq 1$：若有叶子则删叶子和边（$n, e$ 同减 1，$f$ 不变）；否则删圈上一边（$e, f$ 同减 1，$n$ 不变）。由归纳假设即得。$\blacksquare$

**推论 39.5**：简单平面图（$n \geq 3$）满足 $e \leq 3n - 6$。故 $K_5$（$10 > 9$）非平面；二部图界 $e \leq 2n - 4$ 给出 $K_{3,3}$（$9 > 8$）非平面。

**定理 39.5**（Kuratowski 定理）：图是平面图 $\iff$ 不含 $K_5$ 或 $K_{3,3}$ 的细分作为子图。必要性由 $K_5, K_{3,3}$ 非平面且细分保非平面性；充分性通过 $3$-连通成分的 Whitney 嵌入定理与轮图分解的归纳证明。

### 39.5 Ramsey 理论

**定义 39.6**（Ramsey 数）：$R(s, t)$ 为最小 $n$ 使 $K_n$ 任意红蓝边染色必含红色 $K_s$ 或蓝色 $K_t$。

**定理 39.6**（Ramsey 定理）：$R(s, t) \leq R(s-1, t) + R(s, t-1)$；$R(s, t) \leq \binom{s+t-2}{s-1}$。

**证明**：取 $n = R(s-1, t) + R(s, t-1)$，取 $v \in V(K_n)$。$v$ 有 $\geq R(s-1, t)$ 条红边或 $\geq R(s, t-1)$ 条蓝边。前者红邻点集中或含红色 $K_{s-1}$（连 $v$ 成 $K_s$）或含蓝色 $K_t$；后者对称。故 $R(s, t) \leq n$。$\blacksquare$

**定理 39.7**（$R(3, 3) = 6$ 的完整证明）：

**证明**：下界 $R(3, 3) > 5$：$C_5$ 外圈染红、补图（也是 $C_5$）染蓝，无单色三角形。上界 $R(3, 3) \leq 6$：$K_6$ 中取 $v$，鸽巢原理给出 $\geq 3$ 条同色边（不妨红），邻点 $\{x, y, z\}$。若其间有红边则与 $v$ 成红色 $K_3$；若全为蓝边则成蓝色 $K_3$。故 $R(3, 3) = 6$。$\blacksquare$

**注记 39.1**：已知 Ramsey 数极少：$R(3,k)$ 对 $k \leq 9$ 已知，$R(4,4)=18$，$R(4,5)=25$。$R(5,5)$ 的确切值至今未知（仅知 $43$--$48$）。